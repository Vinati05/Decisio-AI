services:
  backend:
    build: ./backend
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports: ["8000:8000"]
  frontend:
    build: ./frontend
    ports: ["3001:3000"]
